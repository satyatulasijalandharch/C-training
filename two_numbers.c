#include <stdio.h>
#include <conio.h>
void main()
{
    int a = 10, b = 20;
    int ans = a + b;
    // %d -> interger
    // %f -> float point value
    // %c -> character
    printf("Addition is : %d", ans);
}